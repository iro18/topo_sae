<form method="post" action="{{ path('app_commentaire_route_delete', {'id': commentaire_route.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire_route.id) }}">
    <button class="btn">Delete</button>
</form>
